<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<!-- Same as entitlements.plist minus get-task-allow, which is
     debug-only and fails notarization. scripts/release.sh re-signs the
     helper with this file; local builds keep the debuggable signature. -->
<dict>
	<key>com.apple.security.device.audio-input</key>
	<true/>
</dict>
</plist>
